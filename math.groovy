def sum(a,b)
{
   echo "The sum of $ {a} and ${b} sum {a+b}"
}
def subtraction(a,b)
{
   echo "the difference of ${a} and ${b} is {a-b}
 }
 def mul(a,b)
 {
   echo "the multi of ${a} and ${b} is {a*b}
  
